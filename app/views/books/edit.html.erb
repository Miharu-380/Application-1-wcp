<h1>Editing Book</h1>

<% if @book.errors.any? %>
    <div class="error_massage">
      <h2><%= @book.errors.count %> prohibited this product from being saved:</h2>
    　<ul>
        <% @book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
<% end %>

<%= form_with model:@book, local:true do |f| %>
<div class="field">
  <%= f.label :title %>
  <%= f.text_field :title %>
</div>
<div class="field">
  <%= f.label :body %>
  <%= f.text_area :body %>
</div>
<div class="botton">
  <%= f.submit 'Update Book' %>
</div>
<% end %>

<%= link_to 'Show',book_path,class:"linkcolor" %>
|
<%= link_to 'Back',books_path,class:"linkcolor" %>